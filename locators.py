class LocatorsElements:
    # Text Box
    element_block_locator = "//h5[text()='Elements']"
    input_permanent_address_locator = "//textarea[contains(@id, 'permanentAddress')]"
    output_permanent_address_locator = "//p[contains(@id, 'permanentAddress')]"
    output_name_locator = "#name"
    output_email_locator = "#email"
    output_currentAddress_locator = "//p[contains(@id, 'currentAddress')]"
    # Check Boxxio

